PHASE 2 FROM SOL -- SESSION 7 HANDOFF
====================================

Date: 20 July 2026
Scope executed: (Q3), then the complete generic a=1 midpoint assembly.


VERDICT
-------

`(Q3)` is PROVED for every prime `p>=11`.  The proof uses the exact
Casoratian of the three companion solutions `(Q,P,Phat)`.  At
`r=(p-5)/2` it is a p-unit unless `p` divides `43*701`; the two exceptional
primes have direct manifest-double-binomial certificates.

Consequently `(U)` holds, `e_p=-5`, and the already proved `(H)`, `(RT)`,
`(RM)`, `(LEAD)`, and Q/Lucas row assemble the saturated midpoint inequality

  v_p(11907P_N-334374P_(N+1)-19292P_(N+2)) >= -4,
  N=(3p-5)/2.

This closes the generic **a=1 band only**.  General `a`, prime-power midpoint
depth, and the separate cubic gates remain open.


Q3 CERTIFICATE
--------------

For the normalized order-three recurrence, the fundamental Casoratian is

  det[(Q,P,Phat)_(n,n+1,n+2)]
    =(-1)^(n-1)a0(n)/
      [16(n+1)^4(n+2)^5(2n+1)(2n+3)C(2n,n)],

  a0(n)=41218n^3+198849n^2+320790n+173057.

This follows uniformly from the companion determinant
`C_(n+1)/C_n=-c0(n)/c3(n)` and the exact base value
`C_1=13591/34560`; it is no longer a finite-pattern claim.

At `r=(p-5)/2`, every denominator factor is a p-unit.  Lemma C makes the
defining base/companion coefficients and harmonic sums p-integral, and the
normalizing `C(2m,m)` is a unit since `2m<p` at the three rows.  Hence the P
and Phat columns are p-integral without invoking the global denominator law.
An all-zero Q column would therefore contradict the unit determinant unless
`p|a0(r)`.  But

  8a0((p-5)/2)
    =41218p^3-220572p^2+397530p-241144,
  241144=8*43*701.

The two exceptional direct certificates are

  p=43:  [Q_19,Q_20,Q_21]=[33,0,26] mod 43,
  p=701: [Q_348,Q_349,Q_350]=[472,350,182] mod 701.

Therefore `(Q3)` holds uniformly.


REQUIRED GATES
--------------

The same manifest evaluator gives exactly

  p=11: [0,5,1],   p=13: [8,5,11],  p=17: [14,10,16],
  p=19: [15,2,0],  p=23: [6,22,13].

Thus the claimed route is gated at all five requested primes.  Notice that
`Q_((p-1)/2)=0 mod 19`; a proof claiming that the last member is always
nonzero would be false.


LITERATURE AUDIT
----------------

MOS 1705.05586 identifies the half-period cellular value with the weight-six
level-four modular coefficient, but that coefficient can vanish modulo p.
Zudilin math/0206178 provides the shared third-order recurrence.  The decisive
nonvanishing input is the recurrence's Casoratian, not an individual closed
form or an ordinarity assertion.


A1 ASSEMBLY
-----------

`(LEAD)` gives `p^5P_(p+t)=(29/28)Q_(p+t) mod p`.  By Lucas, `(Q3)`, and the
fact that `Q_1=21` is a unit for `p>=11`, one of the three leading P digits is
a unit, while the existing floor puts all three in `p^-5 Z_p`; hence `(U)`
and `e_p=-5`.

In `(DET-MID)`, the Q row vanishes mod p, `(H)` places the head error in p^3,
`(RT)` places the reflected regular determinant in p, and `(RM)` places the
middle determinant in p^4.  Therefore `p^5M_p in pZ_p`, i.e.
`v_p(M_p)>=-4=e_p+1`.  This is the complete saturated a=1 midpoint gate.


FILES WRITTEN OR UPDATED
------------------------

  worthiness/sol_q3.py
  worthiness/PHASE2_Q3_CERTIFICATE.md
  worthiness/PHASE2_A1_MIDPOINT_THEOREM.md
  worthiness/SOL_WORKLOG.md (append only)
  worthiness/PHASE2_FROM_SOL_7.txt


Signed,

Sol
20 July 2026
